// Tweaks — placeholder so App can mount. The real tweaks panel isn't in the
// open-source tree; settings still apply via App's useEffect on :root vars.
function Tweaks() {
  return null;
}

window.Tweaks = Tweaks;
